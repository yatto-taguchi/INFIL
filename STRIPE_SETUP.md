# Stripe Checkout 決済API 設定・本番化ガイド

本サイト（INFILL）は、**Stripe Checkout（初期費用0円・月額0円・決済手数料約3.6%）**に対応しています。
以下の手順に沿ってAPIキーを取得・設定するだけで、**Apple Pay / Google Pay / クレジットカード決済**が即座に利用可能になります。

---

## 全体の流れ（約10分で完了）

1. [Stripeアカウントの作成（無料）](#step-1-stripeアカウントの作成無料)
2. [APIキー（テスト用 / 本番用）の取得](#step-2-apiキーの取得)
3. [Vercelの環境変数に登録](#step-3-vercelの環境変数に登録)
4. [テスト決済の動作確認](#step-4-テスト決済の動作確認)
5. [本番利用開始（ライブモード）](#step-5-本番利用の開始)

---

## Step 1: Stripeアカウントの作成（無料）

1. [Stripe 公式サイト（https://stripe.com/jp）](https://stripe.com/jp) にアクセスします。
2. 「今すぐ始める」または「サインアップ」をクリックし、メールアドレス・お名前・パスワードを入力してアカウントを作成します。
3. 登録メール宛てに届く確認リンクをクリックして認証を完了します。

> **NOTE:** テストモード（検証）だけであれば、法人登記や本人確認書類の提出なしで**即日・数分**で始められます。

---

## Step 2: APIキーの取得

1. [Stripe ダッシュボード](https://dashboard.stripe.com/) にログインします。
2. 画面右上のスイッチが **「テスト環境（Test Mode）」** になっていることを確認します（オレンジ色のタグが表示されます）。
3. 画面上部の検索バーで「APIキー」と検索するか、左メニューの **「開発者」 > 「APIキー」** を開きます。
4. 以下のキーを確認します：
   - **公開可能キー（Publishable Key）**: `pk_test_...`
   - **シークレットキー（Secret Key）**: `sk_test_...` （「シークレットキーを表示」をクリックしてコピー）

> **CAUTION:** `sk_test_...` や `sk_live_...` は秘密鍵です。GitHubのコード内に直接書き込んではいけません。必ず次のStep 3のようにVercelの環境変数に設定してください。

---

## Step 3: Vercelの環境変数に登録

1. [Vercel ダッシュボード](https://vercel.com/dashboard) にログインします。
2. 本プロジェクト（`INFIL`）を選択します。
3. 上部タブの **「Settings」 > 「Environment Variables」** を開きます。
4. 以下の環境変数を追加します：
   - **Key**: `STRIPE_SECRET_KEY`
   - **Value**: コピーしたシークレットキー（例: `sk_test_51...`）
   - **Environment**: 「Production」「Preview」「Development」のすべてにチェック
5. **「Save」** をクリックします。
6. 設定を反映するため、上部タブの **「Deployments」** から最新のデプロイの右端メニュー（`...`）をクリックし、**「Redeploy」** を実行します（数十秒で完了します）。

---

## Step 4: テスト決済の動作確認

1. 公開サイト（[https://infil-design.vercel.app](https://infil-design.vercel.app)）を開きます。
2. ロッドスタンドをカートに追加し、**「Stripeで安全に注文決済へ」** をクリックします。
3. Stripe公式の高セキュリティ決済画面（checkout.stripe.com）に自動遷移します。
4. テスト用カード情報を使って決済を完了します：
   - **カード番号**: `4242 4242 4242 4242` （Stripe公式の安全なテスト番号）
   - **有効期限**: 未来の任意の年月（例: `12/28`）
   - **CVC**: 任意の3桁（例: `123`）
   - **お名前・配送先住所**: 任意の住所
5. 決済完了後、INFILLの **注文完了画面（`success.html`）** に自動的に戻り、カートがクリアされればテスト成功です！

---

## Step 5: 本番利用の開始

テストで問題がなければ、実際のお客様から売上を受け取るための本番化を行います：

1. Stripe ダッシュボードで **「本番環境利用の申請（アカウントの有効化）」** を行います（事業主情報、銀行口座情報の入力）。
2. 審査完了後、ダッシュボード右上のスイッチを **「本番環境（Live Mode）」** に切り替えます。
3. **「開発者」 > 「APIキー」** から本番用シークレットキー（`sk_live_...`）をコピーします。
4. Vercelの環境変数 `STRIPE_SECRET_KEY` の値を `sk_live_...` に更新し、Redeployを実行すれば、本物の決済受付がスタートします！

---

## 補足仕様

- **対応ブランド**: VISA, Mastercard, JCB, American Express, Diners Club, Discover
- **デジタルウォレット**: Apple Pay（iOS/Safari）、Google Pay（Android/Chrome）
- **セキュリティ基準**: PCI DSS レベル1（最高位セキュリティ）に完全準拠
